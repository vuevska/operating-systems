#!/bin/bash

# Да се најдат сите активни tcp конекции кои имаат
# локална адреса 127.0.0.1 и за нив да е испечати портата
# кај локалната адреса и Inode одделени со едно празно место

netstat -lnetu | grep tcp | grep 127\.0\.0\.1 | awk '{print $4, $NF}' | awk -F\: '{print $2, $3}'
